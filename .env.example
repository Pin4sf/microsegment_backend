        PROJECT_NAME="Micro Segment Backend (Dev)"
        API_V1_STR="/api/v1"

        SHOPIFY_API_KEY="YOUR_SHOPIFY_APP_API_KEY"
        SHOPIFY_API_SECRET="YOUR_SHOPIFY_APP_API_SECRET"
        SHOPIFY_API_VERSION="2025-04" # Or your target version
        SHOPIFY_APP_SCOPES="read_products,read_orders,read_customers" # Comma-separated
        SHOPIFY_APP_URL="https://your-ngrok-or-app-domain.com"
        SHOPIFY_REDIRECT_URI="https://your-ngrok-or-app-domain.com/api/v1/auth/callback"

        # Generate strong random strings for these (e.g., openssl rand -hex 32)
        APP_SECRET_KEY="YOUR_STRONG_APP_ENCRYPTION_KEY"
        SESSION_SECRET_KEY="YOUR_STRONG_SESSION_SIGNING_KEY"

        # For development, using default from config.py. Override if needed.
        DATABASE_URL="postgresql+asyncpg://microsegment_user:1234@localhost:5432/microsegment_app_db"
        # For production, use your actual production database URL:
        # DATABASE_URL="postgresql+asyncpg://prod_user:prod_password@prod_host:port/prod_db"